export default {

  // ----------------
  // App name & alias
  // ----------------
  name: 'Client App User',
  alias: 'vuestrap-testing-app',
  platform: 'Vuestrap Testing',

  // ---------------------------------------------------------------------------
  // App settings (grouped by component -or- resource)
  // ---------------------------------------------------------------------------
  // Core configuration settings for the app.
  // These values cannot be altered by the user.
  // ---------------------------------------------------------------------------
  settings: {
    // ----------------
    // Project settings
    // ----------------
    project: {
      defaultPageSize: 20,
    },
    // ------------------
    // MapConfig settings
    // ------------------
    mapConfig: {
      defaultPageSize: 20,
    },
  },

}
